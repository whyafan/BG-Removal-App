# Server (Legacy)

This directory contains the Express server used before moving to Vercel
Serverless Functions (`/api`).

It is kept for reference while we migrate. New backend changes should go in
`/api` unless we explicitly decide to switch back to a VPS deployment.
